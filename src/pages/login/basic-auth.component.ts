
import { Component } from "@angular/core";
import { NavParams } from "ionic-angular";

@Component({
    selector: 'component-basic-auth',
    templateUrl : 'basic-auth.html'
})
export class BasicAuthComponent {
    
    constructor(params: NavParams) {
    }
}